<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.indieAndBob.mapper.reward">

   <select id="selectRewardById" parameterType="String" resultType="reward">
   	  select * from user where reward_id=#{rewardId}
   </select>
   <insert id="registerReward" parameterType="reward">
      insert into user (email, name, password, postcode,address,extra_address,bank_name,bank_account,
      is_developer,phone_number,profile,introduction) 
      values (#{email}, #{name}, #{password}, #{postcode},#{address},#{extraAddress},#{bankName},#{bankAccount},
      #{isDeveloper},#{phoneNumber},#{profile},#{introduction})
   </insert>
</mapper>